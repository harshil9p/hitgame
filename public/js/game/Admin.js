/* Admin constructor (client) which is initated to store into the socket list of players */

var Admin = function(id, name) {
    
    /* init() */
    /* set ids and name */
    this.id = id;
    this.name = name;
};