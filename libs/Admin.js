/* Admin constructor (server) which is initated  */

var Admin = function(id, name) {
    
    /* init() */
    /* set ids and name */
    this.id = id;
    this.name = name;

};

exports.Admin = Admin;